{% extends 'base.html.twig' %}

{% block title %}Friends{% endblock %}

{% block body %}
    <h1>Friends</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ friend.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ friend.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ friend.email }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ friend.ville }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('friends_index') }}">back to list</a>

    <a href="{{ path('friends_edit', {'id': friend.id}) }}">edit</a>

    {{ include('friends/_delete_form.html.twig') }}
{% endblock %}
